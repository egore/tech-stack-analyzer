# Technology Type Definitions
# Determines default component behavior for each technology type
# Individual rules can override with is_component field

types:
  # Infrastructure & Data - CREATE components
  database:
    is_component: true
    description: "Database systems (PostgreSQL, MongoDB, Redis, etc.)"
  
  storage:
    is_component: true
    description: "Storage services (S3, Cloudinary, etc.)"
  
  queue:
    is_component: true
    description: "Message queues (RabbitMQ, Kafka, etc.)"
  
  # Hosting & Cloud - CREATE components
  hosting:
    is_component: true
    description: "Hosting platforms (Vercel, Netlify, AWS, etc.)"
  
  cloud:
    is_component: true
    description: "Cloud providers (AWS, GCP, Azure, etc.)"
  
  # Services - CREATE components
  saas:
    is_component: true
    description: "SaaS platforms"
  
  cms:
    is_component: true
    description: "Content management systems"
  
  monitoring:
    is_component: true
    description: "Monitoring services (Datadog, Sentry, etc.)"
  
  analytics:
    is_component: true
    description: "Analytics platforms (Google Analytics, Mixpanel, etc.)"
  
  auth:
    is_component: true
    description: "Authentication services (Auth0, Clerk, etc.)"
  
  payment:
    is_component: true
    description: "Payment processors (Stripe, PayPal, etc.)"
  
  notification:
    is_component: true
    description: "Notification services (SendGrid, Twilio, etc.)"
  
  communication:
    is_component: true
    description: "Communication tools (Slack, Discord, etc.)"
  
  collaboration:
    is_component: true
    description: "Collaboration platforms"
  
  # Applications & Tools - CREATE components
  app:
    is_component: true
    description: "Applications (Nginx, Redis, etc.)"
  
  etl:
    is_component: true
    description: "ETL tools"
  
  automation:
    is_component: true
    description: "Automation tools"
  
  security:
    is_component: true
    description: "Security services"
  
  maps:
    is_component: true
    description: "Map services (Google Maps, Mapbox, etc.)"
  
  crm:
    is_component: true
    description: "CRM systems"
  
  network:
    is_component: true
    description: "Network services"
  
  ssg:
    is_component: true
    description: "Static site generators (when used as main tech)"
  
  ai:
    is_component: true
    description: "AI services (OpenAI, Anthropic, etc.)"
  
  agentic:
    is_component: false
    description: "Agentic AI frameworks (PydanticAI, LangChain, CrewAI, etc.)"
  
  # Development Tools - DO NOT create components
  ci:
    is_component: false
    description: "CI/CD tools (GitHub Actions, Jenkins, etc.)"
  
  vcs:
    is_component: false
    description: "Version control systems (Git, SVN, Mercurial, etc.)"
  
  language:
    is_component: false
    description: "Programming languages (Python, JavaScript, etc.)"
  
  runtime:
    is_component: false
    description: "Runtime environments (Node.js runtime, Python runtime, etc.)"
  
  tool:
    is_component: false
    description: "General development tools"
  
  framework:
    is_component: false
    description: "Application frameworks (React, Django, Spring, etc.)"
  
  validation:
    is_component: false
    description: "Validation libraries"
  
  builder:
    is_component: false
    description: "Build tools (Webpack, Vite, etc.)"
  
  linter:
    is_component: false
    description: "Code linters (ESLint, Pylint, etc.)"
  
  test:
    is_component: false
    description: "Testing frameworks (Jest, Pytest, etc.)"
  
  orm:
    is_component: false
    description: "Object-relational mappers (SQLAlchemy, Prisma, etc.)"
  
  package_manager:
    is_component: false
    description: "Package managers (npm, pip, cargo, etc.)"
  
  ui:
    is_component: false
    description: "UI component libraries"
  
  ui_framework:
    is_component: false
    description: "UI frameworks (MFC, Qt, etc.)"
  
  iac:
    is_component: false
    description: "Infrastructure as Code tools (Terraform, Pulumi, etc.)"
  
  iconset:
    is_component: false
    description: "Icon libraries (Lucide, Font Awesome, etc.)"
  
  build:
    is_component: false
    description: "Build tools (Maven, Gradle, etc.)"
  
  logging:
    is_component: false
    description: "Logging libraries (Log4j, Logback, SLF4J, etc.)"
  
  appserver:
    is_component: true
    description: "Application servers (JBoss, Tomcat, WebLogic, etc.)"
  
  healthcare:
    is_component: false
    description: "Healthcare standards and systems (FHIR, HL7, DICOM, etc.)"
